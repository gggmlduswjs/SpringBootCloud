<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="b">

    <!--   <![CDATA[  ]]>  XML 안에서 “문자 그대로(raw text) 처리해라 의미
           복잡하게 볼 필요 없다
    -->
    <select id="selectPage" resultType="Member" parameterType="map">
        <![CDATA[
        SELECT m_id, m_pw, m_name, m_tel, m_birthday, m_point, m_grade
        FROM (
            SELECT ROWNUM AS num, m_id, m_pw, m_name, m_tel, m_birthday,  m_point, m_grade
            FROM member_tbl_11
        )
        WHERE num BETWEEN #{start}  AND #{end}
       ]]>
    </select>


    <select id="selectPageTest" resultType="Member" >

        SELECT m_id, m_pw, m_name, m_tel, m_birthday, m_point, m_grade
        FROM (
            SELECT ROWNUM AS num, m_id, m_pw, m_name, m_tel, m_birthday,  m_point, m_grade
            FROM member_tbl_11
        )
        WHERE num BETWEEN  1  AND 10

    </select>



    <select id="selectCount"   resultType="int">
        SELECT  count(*) FROM member_tbl_11
    </select>



    <select id="testSelect"   resultType="Member">
        select *
        from
        ( select  rownum num,  m_id, m_pw, m_name, m_tel, m_birthday, m_point , m_grade
        from  member_tbl_11
        )where  num  between  4 and  6
    </select>




    <select id="SelectPageFinal"   resultType="Member"  parameterType="map">
        select *
        from
        ( select  rownum num,  m_id, m_pw, m_name, m_tel, m_birthday, m_point , m_grade
        from  member_tbl_11
        )where  num  between  #{start} and  #{end}
    </select>
</mapper>